@model IEnumerable<Imdb.DATA.Concrete.News>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NewsTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NewsContent)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NewsPhoto)
        </th>
        <th>
            @Html.DisplayName("Edit")
        </th>
        <th>
            @Html.DisplayName("Details")
        </th>
        <th>
            @Html.DisplayName("Delete")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NewsTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NewsContent)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NewsPhoto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.NewsID }, new { @class = "btn btn-success" })
        </td>
        <td>
            @Html.ActionLink("Details", "Details", new { id = item.NewsID }, new { @class = "btn btn-info" })
        </td>
        <td>
            @Html.ActionLink("Delete", "Delete", new { id = item.NewsID }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
